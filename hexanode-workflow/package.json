{
  "name": "hexanode-workflow",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "bun run cre-setup 2>/dev/null || true",
    "postinstall": "bunx cre-setup || true"
  },
  "dependencies": {
    "@chainlink/cre-sdk": "^1.0.7",
    "viem": "^2.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
